#accpet km data and conver into meter and cm

km_data = float(input("enter the km data "))
m_data = km_data * 1000
cm_data = m_data * 100

print(f'your given data is : {km_data}Km, in meter : {m_data} and in centimet {cm_data}')                
